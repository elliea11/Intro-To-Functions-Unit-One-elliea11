""" #define the function

def add(x,y):
    #variables here only accessible in the function, SCOPE
    return x + y 
#call the function
z = add(5,15) """

""" #i is an incrementor/iterator
for i in range(1,3):
    print(i)

    def kevin(x):
        y = 10
        for i in range (x):
            y -= 1
            print(i,y)

    kevin(5)
    kevin(6) """

""" #String for characters
name = "Inaya"
print(name.lower().capitalize())
print("X".isupper())
#I is different to the computer than i
#input asks the user a question and records the answer
#what we write in imput argument is what user sees
#input ALWAYS outputs a string
bill = int(input("How much was the bill"))
print(bill)

if bill == 10:
    print("Match")
else:
    print("no match")

#integer
amt = 100
#Float uses decimal
amt_two = 99.99

#Boolean
x = True
y = False """

bill = input("how much was the bill?)
print(int(bill) + 20)